{% extends 'baseProfile.html.twig' %}




{% block body %}
<div class="container d-flex justify-content-center align-items-center mt-5">
    <div class="certificat-container border border-dark p-5 shadow-lg bg-white rounded text-center">
        
        <!-- Logo en haut -->
        <div class="mb-4">
            <img src="{{ asset('img/logo.png') }}" alt="Logo" width="120">
        </div>

        <!-- Titre principal -->
        <h1 class="text-uppercase font-weight-bold text-dark">Certificat D'achèvement</h1>
        <hr class="border-dark w-50 mx-auto">

        <!-- Corps du certificat -->
        <p class="mt-3 text-muted">Ce certificat est décerné à :</p>
        <h2 class="font-weight-bold text-primary">{{ certificat.userName }} </h2>
        <p class="mt-3 text-muted">Pour avoir complété avec succès la formation :</p>
        <h3 class="font-weight-bold text-success">{{ certificat.formation.titre }}</h3>

        <!-- Date & Signature -->
        <div class="mt-4 d-flex justify-content-between px-5">
            <div class="text-left">
                <strong>Date d'obtention :</strong> {{ certificat.dateIssued|date('d/m/Y') }}
            </div>
            <div class="text-right">
                <strong>Signature :</strong> <img src="{{ asset('img/signature.png') }}" alt="Logo" width="120">
            </div>
        </div>
        <br>
        <!-- Bouton de retour et impression -->

        <div class="mt-4">
            <button onclick="window.print()" class="btn btn-primary ml-2">
                Imprimer le certificat 🖨️
            </button>
            <br>
            <br>
            <a href="{{ path('formation_show', {id: certificat.formation.id}) }}" class="btn btn-secondary">
                Retour à la formation
            </a>
            
            
        </div>
    </div>
</div>

<!-- CSS interne -->
<style>
    .certificat-container {
        width: 800px;
        border: 10px solid #2c3e50;
        padding: 40px;
        background: linear-gradient(to bottom, #f8f9fa, #ffffff);
        font-family: 'Georgia', serif;
    }
    h1, h2, h3 {
        font-family: 'Times New Roman', serif;
    }
    @media print {
        .btn { display: none; }
        body { background: white !important; }
    }
</style>

{% endblock %}