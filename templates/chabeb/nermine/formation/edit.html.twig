{% extends 'baseback.html.twig' %}

{% block title %}Modifier la formation{% endblock %}

{% block body %}
<h1>{% if form.vars.value.id %}Modifier{% else %}Créer{% endif %} une Formation</h1>

{{ form_start(form,{'attr':{'novalidate':'novalidate'}}) }}
    {{ form_widget(form) }}
    
{{ form_end(form) }}

<a href="{{ path('formation_index') }}">Retour à la liste</a>
{% endblock %}
