<form method="post" action="{{ path('psy_delete', {'id': psy.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce Psy ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ psy.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>